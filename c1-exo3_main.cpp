// Programme sain, base de l'expérience. On y introduit UNE faute à la fois
// (voir c1-exo3_reponse.md), on recompile, on relève le message, puis on la retire.
#include <cstdio>

int main()
{
    printf("Bonjour\n");
    return 0;
}
